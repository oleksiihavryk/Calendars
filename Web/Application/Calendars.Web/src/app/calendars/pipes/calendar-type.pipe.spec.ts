import { CalendarTypePipe } from './calendar-type.pipe';

describe('CalendarTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CalendarTypePipe();
    expect(pipe).toBeTruthy();
  });
});
