import { ResourcesHttpClient } from './resources-http-client';

describe('ResourcesHttpClient', () => {
  it('should create an instance', () => {
    expect(new ResourcesHttpClient()).toBeTruthy();
  });
});
